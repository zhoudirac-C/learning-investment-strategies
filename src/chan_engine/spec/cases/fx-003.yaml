# FX-003 经包含处理后的分型识别（课62）
# claim-20070630-001-b：分型等后续识别都在包含处理后的图形上进行。
# claim-20070630-001-a：分型双条件定义。
# 构造：bar2 包含 bar1（11.3>11.0 且 9.8<9.9），方向向上（g1=11.0 >= g0=10.4），
#   向上合并得 M=[h 11.3, l 9.9]——极值高 11.3 来自后一根原始 bar2，
#   与 INCLUDE-001 互补（那里极值来自前一根）。
#   合并后序列 [10.4,9.6] / [11.3,9.9] / [10.9,9.7] 恰成顶分型；
#   不合并则无分型（bar1 高点 11.0 < bar2 高点 11.3，双最高不成立）。
# idx 约定：贡献极值的原始 bar → idx=2。末尾未确认 → sure: false。
case_id: FX-003
claim_refs:
  - claim-20070630-001-b
  - claim-20070630-001-a
bars:
  - [10.0, 10.4, 9.6, 10.1]
  - [10.1, 11.0, 9.9, 10.7]
  - [10.7, 11.3, 9.8, 10.4]
  - [10.4, 10.9, 9.7, 10.6]
expect:
  fx:
    - {idx: 2, type: up, sure: false}

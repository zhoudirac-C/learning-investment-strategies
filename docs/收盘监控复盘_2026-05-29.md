# 收盘监控复盘：2026-05-29

**日期**：2026-05-29（周五）  
**生成时间**：2026-05-29 15:20 CST  
**当前框架**：情绪拐点确认期 + 做T节点  
**数据来源**：`state.json` decision_log + `--live-analysis-context` 行情快照

---

## 一、核心结论（5条）

1. **早盘"进攻回流"为典型开盘诱多误报**：09:17-09:26 连续触发 7 条"进攻回流观察"，但随后市场单边下跌，科创50收跌-5.04%、创业板指-2.11%。这些信号基于开盘瞬间的竞价/高开数据，未经过持续时长过滤，属于**时间维度不足的伪信号**。

2. **"防御切换观察"为今日唯一有效信号**：09:39 首次触发后持续至收盘（共 38 条），防御银行（招行+2.4%/工行+2.56%/平安银行+2.53%）与白酒（茅台+3.92%）全天强势，与科技大跌形成鲜明对照。信号与市场微观结构一致。

3. **持仓存在严重漏报**：安泰科技跌停（-9.99%）、艾华集团大跌（-6.79%），但监控全程静默。原因是 `positions.yaml` 未给这两只持仓配置 `reduce_zone` 或 `risk_zone`/`risk_line`。

4. **指数中间档位存在规则缺失型漏报**：收盘 4068.57，已跌破 `weak_close_level=4080` 和 `trend_defense_proximity=4070`，但未触发任何指数级别提醒。`index_rules` 采用通用格式（`trigger_condition: close_below`），代码可能未完整实现该格式的触发逻辑。

5. **去重机制基本合理，但早盘 18 秒间隔的去重暴露了监控轮询频率过高**：09:17:10 与 09:17:28 同一指纹几乎同时触发，说明轮询间隔过密（约 18 秒），在价格未实质变化时产生重复计算。

---

## 二、提醒质量评估

### 2.1 已发送提醒分类统计

| 动作类型 | 数量 | 时间分布 | 有效性判断 |
|:---|:---|:---|:---|
| 进攻回流观察 | 20 | 09:17-09:26（7条）、10:10（1条）、13:10（1条）、14:00（1条）、14:55（1条） | ⚠️ 早盘 7 条为**存疑/误报**；午后 3 条为**有效**（与尾盘科技小幅反抽对应） |
| 防御切换观察 | 38 | 09:39-14:55 持续触发 | ✅ **有效**，与市场风格切换一致 |

### 2.2 早盘"进攻回流"误报分析

**证据链**：
- 09:17:10 触发第一条：CPU/涨价链/液冷 均涨幅 +3.603%、红盘率 0.889
- 09:18:06 均涨幅仍 +3.595%，09:18:35 +3.269%，09:19:04 +2.497%...
- 但到 09:39:40，市场已转为"防御切换观察"（防御稳定线 均涨幅+0.053% 强于 算力租赁）
- 收盘时：科创50 -5.04%、创业板指 -2.11%、上证 -0.73%

**误报原因**：
1. **sector_rotation_rules.min_spread_pct=1.0% 阈值过低**：开盘竞价/高开的瞬间波动即可触发，未要求持续时长（如"连续 3 分钟维持"）。
2. **进攻组内部结构失真**：早盘进攻组红盘率高是因为部分标的（如 ST得润+3.64%、江海股份高开）的竞价惯性，而非真实买盘支撑。开盘后迅速瓦解。
3. **缺少与指数的交叉验证**：早盘上证开在 4110（+0.29%），但 15 分钟内即回落至 4090 以下。若增加"指数同时站稳分时均线"作为辅助条件，可过滤掉多数诱多信号。

### 2.3 防御切换信号有效性验证

| 时间 | 触发内容 | 收盘验证 |
|:---|:---|:---|
| 09:39:40 | 防御稳定线 均涨幅+0.053%、红盘率0.667 强于 算力租赁 | ✅ 算力租赁（光环新网-5.86%/奥飞数据-5.8%）全天弱势 |
| 10:00:06 | 防御稳定线 均涨幅+0.657%、红盘率1 强于 算力租赁 | ✅ 防御银行持续走强 |
| 14:55:04 | 防御稳定线 均涨幅+2.377%、红盘率1 强于 CPU/涨价链/液冷 | ✅ 进攻组收盘均涨幅-1.402%，红盘率仅0.5 |

---

## 三、漏报检查

### 3.1 指数级别漏报

| 规则 | 阈值 | 收盘值 | 是否应触发 | 实际状态 | 漏报原因 |
|:---|:---|:---|:---|:---|:---|
| weak_repair_alert | 收盘跌破 4080 | 4068.57 | ✅ 应触发 | ❌ 未触发 | `index_rules` 通用格式可能未被代码完整支持 |
| trend_defense_proximity | 收盘跌破 4070 | 4068.57 | ✅ 应触发 | ❌ 未触发 | 同上 |
| 4055_support | 收盘跌破 4055 | 4068.57 | ❌ 未触发（守住） | — | 正常 |

**排查建议**：运行 `python -m qing_investment.stock_monitor --ignore-trading-time` 模拟触发，验证 `trigger_condition: close_below` 是否在非交易时段被代码识别。

### 3.2 持仓级别漏报

| 标的 | 代码 | 收盘涨幅 | 成本 | 当前价 | 应有动作 | 实际提醒 | 漏报原因 |
|:---|:---|:---|:---|:---|:---|:---|:---|
| 安泰科技 | 000969.SZ | **-9.99%** | 25.885 | 22.97 | 风控观察 | ❌ 无 | `positions.yaml` 未配置 `risk_zone`/`risk_line` |
| 艾华集团 | 603989.SH | **-6.79%** | 36.543 | 35.81 | 减仓/风控观察 | ❌ 无 | `positions.yaml` 未配置 `risk_zone`/`risk_line` |

**关键发现**：`positions.yaml` 的 `positions` 列表中，安泰科技和艾华集团仅有 `code/name/shares/cost/account/open_date/note` 字段，**完全缺失 `reduce_zone` 和 `risk_zone`/`risk_line`**。这导致 `evaluate_position_alerts()` 遍历持仓时没有任何价格区间可比对，持仓监控形同虚设。

### 3.3 观察池级别漏报（非规则缺失，但值得记录）

观察池标的（如芯联集成-10.24%、华虹公司-5.2%、飞荣达-7.7%）今日大跌，但监控脚本**设计上不对观察池做自动价格提醒**（仅在大模型分析时纳入上下文）。这符合当前架构，但若用户期望观察池大跌时收到"证伪提醒"，需在 `strategy_pack.yaml` 中新增 `watchlist_alert_rules`。

---

## 四、去重合理性总结

### 4.1 今日被压制信号（3条）

| 时间 | 动作 | 触发内容 | 与上一条间隔 | 判断 |
|:---|:---|:---|:---|:---|
| 09:17:28 | 进攻回流观察 | CPU/涨价链/液冷 均涨幅+3.603% 强于 防御稳定线 | 距 09:17:10 仅 **18秒** | ✅ **合理**。同一指纹，价格几乎无变化，属于轮询频率过高导致的重复计算 |
| 09:19:04 | 进攻回流观察 | CPU/涨价链/液冷 均涨幅+2.497% 强于 防御稳定线 | 距 09:19:04（另一条规则 emitted）同时 | ⚠️ **存疑**。该指纹与 09:18:56 的指纹不同（涨幅从 2.679%→2.497%），但去重窗口内被压制。实际上 09:19:04 已有另一条规则（price_increase_vs_cpo）被发出，信息已传递，压制合理 |
| 10:00:08 | 防御切换观察 | 防御稳定线 均涨幅+0.657% 强于 算力租赁 | 距 10:00:06 仅 **2秒** | ✅ **合理**。同一指纹，2秒间隔显然是重复轮询 |

### 4.2 去重机制的问题与建议

**问题**：`dedupe_minutes=30` 的默认窗口对板块轮动信号是合理的，但**轮询间隔过密**（18秒）导致同一状态被多次计算。`state.json` 中 09:17:10 和 09:17:28 的指纹完全相同、价格完全相同，说明监控脚本的调度间隔设置过短。

**建议**：
- 检查 cron 或调度器的触发间隔，建议**不低于 60 秒**（当前似乎为 15-20 秒）。
- 或在 `filter_new_alerts()` 中增加"价格变化率<0.1% 且间隔<60秒"的二次过滤，避免纯时间重复。

---

## 五、YAML 配置调整建议

### 5.1 `positions.yaml` — 补充持仓风控区间（**最高优先级**）

当前持仓缺少价格区间配置，导致跌停/大跌无提醒。**必须立即补充**：

```yaml
accounts:
  - name: "账号1"
    positions:
      - code: "000969.SZ"
        name: "安泰科技"
        shares: 200
        cost: 25.885
        account: "账号1"
        open_date: "2026-05-29"
        note: "新开仓"
        reduce_zone: "23.5-24.5"      # 新增：减亏区（成本下方约5-8%）
        risk_zone: "22.0-22.5"        # 新增：风控线（今日跌停价22.97附近）
        
  - name: "账号2"
    positions:
      - code: "603989.SH"
        name: "艾华集团"
        shares: 100
        cost: 36.543
        account: "账号2"
        open_date: "2026-05-29"
        note: "新开仓"
        reduce_zone: "34.0-35.0"      # 新增：减亏区
        risk_zone: "33.0-33.5"        # 新增：风控线
```

> **注意**：`risk_line: 22.0` 会被解析为单点 `(22.0, 22.0)`，触发条件为 `latest <= 22.0`。若期望区间触发，必须使用 `risk_zone: "22.0-22.5"` 格式。

### 5.2 `strategy_pack.yaml` — 指数规则格式修复

当前 `index_rules` 使用通用格式，但代码可能未完整支持。建议**增加 legacy 格式作为兜底**，或确认代码已支持 `trigger_condition: close_below`。

**修改方案 A（增加 legacy 兜底）**：
```yaml
index_rules:
  # 保留现有通用格式（未来兼容）
  - name: "4055_support"
    index: "上证指数"
    trigger_condition: "close_below"
    threshold: 4055
    ...
  
  # 增加 legacy 格式确保触发
  - trend_defense: 4070
  - weak_close_level: 4080
```

**修改方案 B（若确认代码支持通用格式）**：
运行 `--ignore-trading-time` 测试确认触发后，保持现状。若测试不触发，则采用方案 A。

### 5.3 `strategy_pack.yaml` — 板块轮动阈值调整

早盘"进攻回流"误报的核心原因是阈值过低 + 无持续时长过滤。建议：

```yaml
sector_rotation_rules:
  - id: "cpu_vs_defense"
    name: "CPU自研链 vs 防御"
    offensive_groups:
      - "cpu_self_development"
      - "upstream_price_increase"
      - "liquid_cooling"
    defensive_groups:
      - "defensive_stabilizers"
    min_spread_pct: 1.5        # 从 1.0 上调至 1.5，过滤开盘瞬态波动
    min_red_ratio_spread: 0.3  # 从 0.25 上调至 0.3
    # 建议新增（需代码支持）：
    # min_duration_seconds: 180  # 持续 3 分钟以上才触发
```

### 5.4 `strategy_pack.yaml` — sector_groups 清理已清仓标的

检查 `avoid_semiconductor` 组：
```yaml
- id: "avoid_semiconductor"
  name: "规避-半导体"
  style: "avoid"
  members:
    - code: "600584.SH"
      name: "长电科技"
    - code: "002185.SZ"
      name: "华天科技"
    - code: "002156.SZ"
      name: "通富微电"    # ⚠️ 已清仓，但仍在组内
```

**建议**：从 `avoid_semiconductor` 中移除 `002156.SZ 通富微电`（已清仓）。虽然该组 style 为 "avoid" 不直接参与进攻/防御轮动计算，但会污染板块强度统计和观察池数据。

### 5.5 `strategy_pack.yaml` — 新增"指数中间档位"提醒

当前代码仅支持 `close_below` 触发，不支持"收盘处于 X-Y 区间"的观察提醒。建议在 `interpretation` 中明确中间档位的含义，并在复盘时手动检查：

```yaml
- name: "middle_zone_alert"
  index: "上证指数"
  trigger_condition: "close_between"  # 需代码新增支持
  threshold_low: 4070
  threshold_high: 4080
  severity: "observe"
  action: "指数中间档位观察"
  interpretation: "收盘4070-4080之间=弱修复确认，持仓以保护为主，不新增净仓"
```

若代码短期无法支持，可在每日 cron 复盘脚本中增加收盘档位判断逻辑。

---

## 六、下一交易日（2026-06-01）最重要的 3 条观察条件

### 条件 1：4055 支撑有效性 — 拐点是否失效

- **触发条件**：上证指数开盘 30 分钟内跌破 4055 且 30 分钟不能收回。
- **证伪条件**：开盘站稳 4060 上方，或跌破后 15 分钟内放量收回。
- **意义**：4055 是博主定义的"拐点有效性底线"。跌破 = 5月28日V型修复证伪，回归5月22日杀跌剧本。

### 条件 2：持仓风控线 — 安泰科技/艾华集团是否继续恶化

- **触发条件**：
  - 安泰科技开盘继续跌停或跌破 22.0（风控线）
  - 艾华集团跌破 33.5（风控线）或 30 分钟不能站稳 35.0
- **证伪条件**：
  - 安泰科技打开跌停且放量回升至 23.5 上方
  - 艾华集团开盘后 30 分钟内收复 36.0
- **意义**：今日两只持仓均大幅亏损（安泰科技浮亏约-11.2%，艾华集团浮亏约-2.0%），若次日继续恶化需执行止损纪律。

### 条件 3：防御风格持续性 — 资金避险还是风格切换确认

- **触发条件**：
  - 防御银行（招行/工行/平安银行）继续收涨 +1% 以上，且红盘率维持 1.0
  - 同时科技方向（科创50/创业板指）继续收跌 -1% 以上
- **证伪条件**：
  - 银行板块高开低走，或科技方向出现放量反弹（科创50 收涨）
  - 茅台/五粮液冲高回落，资金回流科技
- **意义**：今日"防御切换"信号有效，但需确认是单日避险（周五+美股不确定性）还是持续风格切换。若防御持续 2 天以上，框架需从"情绪拐点+做T"调整为"防御为主+降低仓位"。

---

## 七、附：今日关键数据速查

| 指标 | 数值 | 备注 |
|:---|:---|:---|
| 上证指数 | 4068.57 | -0.73%，跌破 4080/4070 |
| 创业板指 | 4037.95 | -2.11% |
| 科创50 | 1751.32 | -5.04%，科技情绪崩溃 |
| 安泰科技 | 22.97 | -9.99%，持仓跌停 |
| 艾华集团 | 35.81 | -6.79%，持仓大跌 |
| 防御银行平均 | +2.5% | 招行+2.4%/工行+2.56%/平安银行+2.53% |
| 茅台 | 1326.00 | +3.92%，白酒避险 |
| 芯联集成 | 8.06 | -10.24%，观察池跌停 |
| 华天科技 | 19.34 | -10.0%，规避组跌停 |

---

*复盘完成。数据时间戳：2026-05-29 15:20 CST。数据源：东方财富 push2 / 腾讯财经 gtimg。*

# INCLUDE-003 连续包含的顺序处理（课65）
# claim-20070716-001-a：包含不具传递性，必须顺序合并——先用第1、2根确认新K线，
#   再用新K线与第3根比，有包含则继续结合。
# 构造关键：bar2 含于 bar1 → 向上合并 M12=[11.2,10.2]；
#   bar3=[11.15,10.25] 与 bar2 无包含（11.15>11.1），但含于 M12（11.15<11.2）——
#   只有按顺序先合并出 M12 才能发现第二次包含；若跳过顺序直接两两比较会漏掉。
#   再向上合并得 M123=[11.2,10.25]（高 11.2 来自原始 bar1）。
#   合并后序列 [10.8,9.6] / [11.2,10.25] / [10.9,9.7] 恰成顶分型。
# idx=1（极值 11.2 所在原始 bar）。末尾未确认 → sure: false。
case_id: INCLUDE-003
claim_refs:
  - claim-20070716-001-a
  - claim-20070630-001-b
bars:
  - [10.0, 10.8, 9.6, 10.5]
  - [10.5, 11.2, 10.0, 11.0]
  - [11.0, 11.1, 10.2, 10.6]
  - [10.6, 11.15, 10.25, 10.4]
  - [10.4, 10.9, 9.7, 9.9]
expect:
  fx:
    - {idx: 1, type: up, sure: false}

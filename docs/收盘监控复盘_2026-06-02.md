# 收盘监控复盘 — 2026-06-02

**数据时间戳**：2026-06-02 15:20:41 CST  
**数据源**：`stock_monitor.py --daily-review-context` + `state.json` + `positions.yaml` + `strategy_pack.yaml`  
**当前框架**：AI科技调整期 + 新旧势力交替混沌轮动 + 大小票分化

---

## 一、核心结论（5条）

1. **持仓提醒整体有效，但存在配置更新时序问题**：今日18条已发提醒中，15条为有效提醒；3条（万通发展减仓×2、安泰科技风控×2）因盘中配置更新（11:14）导致前后阈值不一致，属"时序有效但 retrospectively 存疑"。
2. **板块轮动阈值过低导致开盘伪信号**：`min_spread_pct=1.0` 在早盘竞价/高开阶段极易触发"进攻回流"，但09:45时CPU自研链均涨幅仅0.054%即被标记为"进攻回流"，时间维度不足，存在诱多误报风险。
3. **科创50跌破1750未触发指数提醒**：`kechuang50_double_top` 使用 `trigger_condition: close_below` 通用格式，代码可能未完整支持，导致1694.60收盘跌破1750却无风险提醒——**规则缺失型漏报**。
4. **去重机制合理但可优化**：仅1条被压制（安泰科技风控 10:30），与上条间隔30分钟、价格从21.7→21.84（小幅回升），压制合理；但价格回升趋势下重复提醒本身意义递减，建议增加"价格远离阈值"自动降频。
5. **sector_groups 与持仓未同步**：`cpu_self_development` 仍含已清仓的万通发展，可能拖累该组涨幅计算，产生错误的板块轮动信号。

---

## 二、提醒质量评估

### 2.1 分类统计

| 动作类型 | 数量 | 时间分布 | 有效性判断 |
|---------|------|---------|-----------|
| 防御切换观察 | 8 | 09:26-13:10 | ⚠️ 5条为防御稳定线>算力租赁（有效），3条为光通信/CPO>上游涨价链（标签混淆） |
| 进攻回流观察 | 3 | 09:45-10:30 | ⚠️ 均涨幅极低（0.054%~0.49%），阈值设计问题导致伪信号 |
| 减仓观察 | 5 | 09:45-14:00 | ✅ 万通×2（时序有效）、安泰×2（有效）、长城×1（有效） |
| 风控观察 | 3 | 10:00-11:00 | ⚠️ 使用旧risk_zone=21.5-22，11:14更新后未再触发（合理） |

### 2.2 重点信号深度分析

**A. 万通发展 减仓观察（09:45、11:00）— ✅ 有效**
- 09:45 price=15.86，reduce_zone=15.5-16 → 价格落入区间，触发正确。
- 11:00 price=15.52，仍在区间内 → 二次提醒合理。
- **关键背景**：该持仓于11:14才被移入 `closed_positions`，此前一直在活跃持仓列表。提醒非配置滞后误报，而是"持仓正在被执行清仓"过程中的正常提醒。
- 11:14后无再提醒 → 配置更新生效，后续行为正确。

**B. 安泰科技 风控观察（10:00、11:00）— ⚠️ 存疑（时序问题）**
- 10:00 price=21.7，触发 `触及或跌破风险线21.5-22`。
- 但当前 `positions.yaml` 中 risk_zone=21.00-21.50，与提醒文本中的 `21.5-22` 不一致。
- **根因**：`positions.yaml` 于11:14被修改，将 risk_zone 从 `21.5-22` 收紧为 `21.00-21.50`。10:00和11:00的提醒使用的是旧配置，在当时是有效的；按新配置则21.7>21.50，不会触发。
- **结论**：提醒在生成时刻有效，但配置更新造成了 retrospective 不一致。建议避免盘中修改风险阈值，或修改后重启监控进程。

**C. 安泰科技 减仓观察（13:10、14:00）— ✅ 有效**
- price=22.17/22.15，reduce_zone=22.00-23.00 → 价格落入区间，触发正确。
- 与当前配置一致，无争议。

**D. 中国长城 减仓观察（11:20）— ✅ 有效**
- price=17.96，reduce_zone=17.50-18.00 → 新持仓首日即落入减仓区，提醒及时。

**E. 板块轮动：进攻回流观察（09:45、10:00、10:30）— ⚠️ 伪信号风险**
- `tech_internal_rotation` 规则：`min_spread_pct=1.5`，`require_offensive_positive=false`。
- 09:45时CPU自研链+上游涨价链均涨幅=0.054%、红盘率=0.429，即被标记为"进攻回流"。
- **问题**：均涨幅仅0.054%，几乎平盘，却被标记为"进攻回流"，语义上严重夸大。`require_offensive_positive=false` 允许"跌得少"被误判为"进攻回流"。
- 10:00时均涨幅=-0.689%（负值！），仍触发"进攻回流"——这是**明显的标签错误**，负涨幅不可能是"回流"。
- **根因**：`tech_internal_rotation` 未设置 `require_offensive_positive: true`，且阈值1.5%在开盘波动期过低。

**F. 板块轮动：防御切换观察 光通信/CPO > 上游涨价链（09:26、11:20、13:10）— ⚠️ 标签混淆**
- `price_increase_vs_cpo` 规则比较的是 `upstream_price_increase + liquid_cooling` vs `cpo_optical`。
- 两组均为 **offensive（进攻性）** 板块，但当CPO更强时，系统输出"防御切换观察"。
- **问题**："防御切换"的语义是"资金从进攻转向防御"，但此处是"进攻板块内部轮动"，标签与微观结构严重不符。
- 09:26时CPO均涨幅1.973%、上游涨价链均涨幅0.294%，差值1.679%——这是科技内部的强弱分化，不是防御切换。

---

## 三、漏报检查

### 3.1 指数漏报

| 规则/标的 | 阈值 | 收盘值 | 是否应触发 | 实际状态 | 漏报原因 |
|----------|------|--------|-----------|---------|---------|
| 科创50双顶 (`kechuang50_double_top`) | close_below 1750 | ~1694.60 | ✅ 应触发 | ❌ 未触发 | 通用格式 `trigger_condition: close_below` 代码可能未完整支持；需用 legacy `trend_defense: 1750` 兜底 |
| 上证4055支撑 (`4055_broken`) | close_below 4055 | ~4061.17 | ❌ 不应触发 | ❌ 未触发 | 正确 |
| 上证4000支撑 (`4000_support`) | close_below 4000 | ~4061.17 | ❌ 不应触发 | ❌ 未触发 | 正确 |
| 上证弱修复 (`weak_repair_alert`) | close_above 4080 | ~4061.17 | ❌ 不应触发 | ❌ 未触发 | 正确（需close_above） |
| 上证趋势位 (`trend_defense_4032`) | trend_defense 4032 | ~4061.17 | ❌ 不应触发 | ❌ 未触发 | 正确 |
| **中间档位（4032-4080）** | — | 4061.17 | ⚠️ 应观察 | ❌ 无提醒 | **规则缺失**：无 `close_between` 或中间档位触发条件 |

### 3.2 持仓漏报

| 标的 | 配置 | 盘中价格 | 是否应触发 | 实际状态 | 漏报原因 |
|------|------|---------|-----------|---------|---------|
| 安泰科技 | risk_zone=21.00-21.50 | 最低~21.7 | ❌ 不应触发 | ❌ 未触发 | 正确（价格未跌破21.50） |
| 中国长城 | risk_zone=16.80-17.20 | ~17.96 | ❌ 不应触发 | ❌ 未触发 | 正确（价格未跌破17.20） |
| 万通发展 | 无risk_zone | ~15.5 | — | ❌ 未触发 | 正确（未配置risk_zone） |

### 3.3 观察池漏报

- 今日观察池未配置自动提醒（仅持仓和板块轮动有规则触发）。
- 无观察池买入/证伪条件被触发 → 无漏报。

---

## 四、去重合理性

| 时间 | 动作 | 内容 | 与上条间隔 | 判断 |
|------|------|------|-----------|------|
| 10:30:43 | 风控观察 安泰科技 | 触及或跌破风险线21.5-22，price=21.84 | 距10:00:55约30分钟 | ✅ **合理**。同一指纹（`风控观察|000969.SZ|安泰科技|触及或跌破风险线21.5-22`），默认 `dedupe_minutes=30` 窗口内。价格从21.7→21.84，小幅回升，重复提醒意义不大。 |

**去重机制总体评价**：今日仅1条被压制，去重窗口设置合理。但存在改进空间：
- 当价格**远离**阈值时（如从21.7回升至21.96，已脱离risk_zone中心），即使指纹相同，也应允许再次提醒以告知"风险解除"。当前去重仅按指纹+时间窗口，未考虑价格变化方向。

---

## 五、YAML配置调整建议

### 5.1 `strategy_pack.yaml` — 指数规则格式修复

**问题**：`kechuang50_double_top` 使用通用格式 `trigger_condition: close_below`，代码可能未完整支持，导致科创50跌破1750未触发。

**建议**：增加 legacy 格式兜底，确保兼容性。

```yaml
index_rules:
  # ... 现有规则保留 ...
  - name: kechuang50_double_top
    index: 科创50
    trigger_condition: close_below
    threshold: 1750
    severity: risk
    action: 科创50双顶调整深化
    trigger_template: 收盘跌破{threshold}
    interpretation: 科创50 120分钟双顶结构，5/30已大跌至1751.32(-5.04%)。若继续跌破1750，调整深化，半导体/科技核心继续承压。
  # 新增 legacy 兜底（通用格式失效时使用）
  - name: kechuang1750_legacy_fallback
    index: 科创50
    trend_defense: 1750
    severity: risk
    action: 科创50跌破1750（legacy兜底）
    interpretation: 科创50跌破1750趋势防线，调整深化。此规则为legacy格式兜底，确保通用格式未触发时仍有提醒。
  # 新增中间档位观察规则
  - name: shanghai_middle_ground
    index: 上证指数
    trigger_condition: close_between  # 需代码支持，或先用 interpretation 手动提醒
    threshold_low: 4032
    threshold_high: 4080
    severity: observe
    action: 上证中间档位震荡
    interpretation: 上证收盘在4032-4080之间，处于趋势位与弱修复位之间的中间档位。不触发方向性判断，以持仓观察和做T为主，等待方向选择。
```

### 5.2 `strategy_pack.yaml` — 板块轮动阈值上调

**问题**：`min_spread_pct=1.0` 过低，开盘阶段极易触发伪信号；`tech_internal_rotation` 未要求进攻组为正即触发"进攻回流"。

**建议**：

```yaml
sector_rotation_rules:
  # ... 现有规则 ...
  - id: tech_internal_rotation
    name: 科技内部分化：CPU/涨价链 vs 半导体
    offensive_groups:
      - cpu_self_development
      - upstream_price_increase
    defensive_groups:
      - avoid_semiconductor
    min_spread_pct: 2.0        # 从1.5上调至2.0，减少开盘噪音
    min_red_ratio_spread: 0.35  # 从0.3上调至0.35
    require_offensive_positive: true  # 新增：进攻组必须为正才能叫"进攻回流"
  - id: cpu_vs_defense
    name: CPU自研链 vs 防御
    offensive_groups:
      - cpu_self_development
      - upstream_price_increase
      - liquid_cooling
    defensive_groups:
      - defensive_stabilizers
    min_spread_pct: 1.5        # 从1.0上调至1.5
    min_red_ratio_spread: 0.3   # 从0.25上调至0.3
    require_offensive_positive: true  # 保持
  - id: compute_rental_vs_defense
    name: 算力租赁 vs 防御
    offensive_groups:
      - compute_rental
    defensive_groups:
      - defensive_stabilizers
    min_spread_pct: 1.5        # 从1.0上调至1.5，减少频繁触发
    min_red_ratio_spread: 0.25  # 从0.2上调至0.25
```

### 5.3 `strategy_pack.yaml` — sector_groups 清理

**问题**：`cpu_self_development` 仍含已清仓的万通发展（600246.SH），可能拖累该组涨幅计算。

**建议**：

```yaml
sector_groups:
  # ... 其他组保留 ...
  - id: cpu_self_development
    name: CPU自研链
    style: offensive
    members:
      - code: 002055.SZ
        name: ST得润
      # 删除：万通发展已清仓，不应再纳入板块轮动计算
      # - code: 600246.SH
      #   name: 万通发展
```

### 5.4 `positions.yaml` — 避免盘中修改阈值

**问题**：11:14盘中修改 `risk_zone` 导致前后提醒阈值不一致，造成复盘困惑。

**建议**：
- **纪律**：交易时段内（09:15-15:00）不修改 `risk_zone`、`reduce_zone` 等阈值字段。
- 如需调整，应在盘前（08:30前）或收盘后（15:30后）统一更新，并重启监控进程。
- 在 `positions.yaml` 顶部增加注释：

```yaml
# ⚠️ 交易时段内禁止修改 risk_zone / reduce_zone
# 如需调整，请在盘前08:30前或收盘后15:30后更新，并重启监控进程
updated_at: '2026-06-02'
```

### 5.5 `strategy_pack.yaml` — 板块轮动标签修正

**问题**：`price_increase_vs_cpo` 规则在CPO强于上游时输出"防御切换观察"，但两组均为进攻板块，标签语义错误。

**建议**：修改规则名称和输出标签，或拆分为独立规则：

```yaml
sector_rotation_rules:
  # 将 price_increase_vs_cpo 重命名为科技内部分化，输出改为"科技内部分化观察"
  - id: tech_internal_cpo_vs_upstream
    name: 科技内部轮动：CPO vs 上游涨价链
    offensive_groups:
      - cpo_optical
    defensive_groups:
      - upstream_price_increase
      - liquid_cooling
    min_spread_pct: 1.5
    min_red_ratio_spread: 0.2
    # 输出标签建议由代码根据 group style 动态判断：
    # 若 offensive_groups 中 style=offensive 的组更强 → "科技内部进攻观察"
    # 若 defensive_groups 中 style=offensive 的组更强 → "科技内部轮动观察"
```

> **注**：标签动态化需要代码修改，短期可先通过 `interpretation` 字段人工说明。

---

## 六、下一交易日最重要的3条观察条件

### 条件1：安泰科技是否跌破21.50且30分钟不能收回
- **触发条件**：安泰科技价格 ≤ 21.50，且30分钟内无法站回21.50上方。
- **证伪条件**：价格触及21.50后迅速收回，或开盘即高于21.50且维持。
- **意义说明**：当前risk_zone=21.00-21.50，21.50是减仓/清仓的纪律线。若跌破且不能收回，按 `positions.yaml` 的 `today_plan` 应触发清仓。纳米晶/软磁板块整体弱势（艾华集团跌停），需确认是板块系统性回调还是个股独立问题。

### 条件2：科创50是否站稳1700点并出现30分钟级别底背离
- **触发条件**：科创50开盘不低于1690，且30分钟内放量回升至1700上方，MACD或RSI出现底背离信号。
- **证伪条件**：科创50开盘即跌破1690，或回升至1700但无量能配合，随后回落。
- **意义说明**：科创50昨日收盘1694.60，已跌破1750双顶颈线。若不能快速修复至1700上方，调整将深化，科技线全面承压，持仓需进一步降仓。1700是短期心理支撑，也是左侧观察区的边界。

### 条件3：MLCC/被动元件组（风华高科+三环集团+火炬电子）是否维持联动且不出现长上影线
- **触发条件**：MLCC组至少2只标的同步上涨，风华高科涨幅≥2%且无长上影线（上影线/实体 < 0.5），三环集团跟涨。
- **证伪条件**：仅单票上涨（如仅风华高科独涨），或出现长上影线（冲高回落），或三环集团继续下跌。
- **意义说明**：MLCC是博主框架中"最确定的中观线索"，但本质是外溢承接而非类存储大紧缺。若MLCC组不能维持联动，则周期上行逻辑被削弱，观察池买点应继续延后。长上影线是抛压信号，需警惕"一致预期后的分歧"。

---

## 附：今日关键数据速查

| 指标 | 数值 | 备注 |
|------|------|------|
| 上证指数 | ~4061.17 | +0.08%，4032-4080区间震荡 |
| 科创50 | ~1694.60 | +1.86%（反弹但仍<1750） |
| 创业板指 | ~4040.59 | +2.27% |
| 安泰科技收盘 | ~22.15 | 减仓区22-23内，未触risk_zone |
| 中国长城收盘 | ~17.96 | 减仓区17.5-18内 |
| 万通发展 | 已清仓 | 15:00前移入closed_positions |
| 已发提醒 | 18 | 含板块轮动+持仓提醒 |
| 被压制 | 1 | 安泰科技风控（30分钟去重） |
| 大模型分析 | 8 | 规则驱动+定时驱动 |

---

*报告生成时间：2026-06-02 15:21 CST*  
*数据截止日期：2026-06-02*  
*免责声明：本报告仅用于监控复盘和配置优化，不构成投资建议。*

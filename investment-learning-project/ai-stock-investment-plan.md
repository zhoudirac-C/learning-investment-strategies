# AI 炒股投资项目方案：从"模仿 UP"到"学会方法论"的自动化投资分析系统

> **版本**：v2.2（2026-08-15，第四稿）
> **设计依据**：以原《投资学习项目框架》（已归档 `docs/archived/investment-learning-framework-v3.0/`）的**核心模块**为蓝本——该框架定义了一套经过思考验证的投资方法论（双路径、假设清单、信号跟踪、事前验尸、仓位规则、复盘校准），但它靠**人手工执行**。本项目的目标是：**把这套方法论的每个核心模块自动化成一个 AI 子系统**，机器按方法论跑流程、人只做最终决策。
> **免责声明**：系统一切产出仅供研究参考，**不构成任何投资建议**；不接入真实交易接口，不自动下单。

> **v2.1 变更说明**：v2.0 解决了"方法论自动化"，但知识底座仍锚定在单一 UP 主（B站博主青枫浦上Q）身上——`knowledge/claims/` 451 张观点卡、`framework/reasoning-patterns.yaml` 的推理模式、`sources/raw/财经/` 550 篇转写稿几乎全部源自其一人，且评测以 UP 为 ground truth。**UP 断更，则系统失准**。本稿核心变更：
> 1. 新增**引擎⓪ 方法论蒸馏**：把 UP 推理模式提纯为来源中立、经市场验证的方法论（第四节）；
> 2. 新增**产业链知识库**为底座中的底座——UP 思想的核心（涨价链推导）本质是跑在产业链客观结构上的推理，而产业链结构由公告/研报/财报/价格供给，**不依赖 UP 持续输出**（第五节）；
> 3. 新增**盲测对齐与毕业机制**：以市场走势为主判据验证 AI 独立结论，达标后毕业脱离（第十节）；
> 4. **prompt 层拆除 UP 实时锚定**，UP 内容降级为训练教材与事后对照标签（第十一节）；
> 5. 知识底座多源分桶 + 市场结果回写置信度（第十二节）。
>
> v2.0 的四大引擎骨架、输出契约、四条纪律铁律、"宁可没建议不给错建议"的降级原则，**全部保留**。
>
> **v2.2 变更说明**（影子首周实证后修订，详见第十六节）：影子双轨跑通 6 个交易日（2026-08-07~08-14），机制按设计兑现（契约 v2→v5 快速迭代、归因挖出数据缺/步骤缺、factcheck 抓到编造），同时暴露 1 个流程 bug 与若干判据缺陷。本稿不变动骨架，只做六处修订：① 毕业判据修正（阶段一致率降权，方向/标的超额升主判据，分环境段达标）；② 重跑-归因联动闭环（修 bug）；③ qing-agent 对比臂直接复用现有 `daily_review_summary.json`（46 天），不做 qing 盲跑；④ 研报管线定案（akshare 东财研报为主力公开源 + KPL 本地关键词初调 + 人工投递；KPL 无搜索端点、TDX 无资讯能力，已实测排除）；⑤ 分析层 skill 化路线（skill = 方法论 + 查询脚本 + 输出契约，影子/盲测继续用确定性整包）；⑥ M1/M2/M3 遗留事项按"立即可做 / 等数据 / 等毕业"重排。

---

## 一、问题诊断：为什么 v2.0 会"UP 断更即失准"

| # | 缺陷 | 证据 |
|---|---|---|
| 1 | **单点依赖**：知识底座几乎全部源自 UP 一人 | 451 张 claims 逐字摘自其 B 站内容；`reasoning-patterns.yaml` 每条模式的 source_raw 均为 UP；研报仅 2 篇 |
| 2 | **评测错配**：只评"像不像 UP"，不评"对不对" | `scripts/evaluate_agent_vs_up.py` 以 UP 为 ground truth；claims 的 confidence 由 LLM 按语言强度设定，无市场背书；evals 自述"不追求预测涨跌" |
| 3 | **推理风格固化**：prompt 要求模仿 UP 思考 | `market_analyst.txt`："推理模式…这是你思考的方式，不是可选工具""你应该模仿这个逻辑框架"；`direction_signals`/`up_sentiment` 实时锚定 UP 情绪；冲突裁决"UP 优先" |
| 4 | **产业分析没底座化**：UP 思想的核心反而是仓库里最散的资产 | 产业链研究散落在 `docs/标的深度研究/` 的一次性快照里（写完不更新），没结构化、没和推理引擎挂钩、没有事件驱动维护；`sector-diffusion-framework.md` 的 BOM 拆解没有可调用的产业链数据支撑 |

**关键洞察**：UP 炒股思想的核心是**产业链推导**——涨价链（AI需求爆发 → 产能向高端倾斜 → 高壁垒供给刚性 → 全品类涨价轮动）是从产业链结构推出来的，不是盘感。而产业链结构（环节分层、价值量分布、壁垒、格局、传导路径）是**客观、缓变、可从公开数据独立维护的知识**——这恰恰是"脱离 UP"最硬的底座：UP 断不断更，长鑫的扩产招标、雅克科技的前驱体份额、CMP 设备的国产唯一性都在那里。把底座从"UP 说了什么"换成"产业链客观上是什么样"，方法层的推理才有独立运行的地基。

## 二、认识论基础：UP 内容三层拆解

UP 的内容不是铁板一块，三层价值命运完全不同：

| 层 | 例子 | 性质 | v2.1 处置 |
|---|---|---|---|
| **观点层** | "明天看好燃气轮机" | 会过期，不是知识 | 只作对照标签，不入知识底座 |
| **方法层** | 涨价链五步拆解、BOM 扩散筛选、冰点/炸板率情绪判断 | 可蒸馏的启发式 | 提纯为来源中立方法论，**必须经市场验证才可信**（引擎⓪） |
| **信息层** | 渠道调研、产业链人脉消息、盘面嗅觉 | AI 物理上没有此渠道 | 用公开数据（板块资金、价格、公告、研报、招标）近似替代；替代不了的**诚实标注能力边界** |

**"彻底脱离"的诚实定义**：方法论可复制的结论类型（产业链推导、信号识别、情绪阶段判断、标的分层）达到可验证的命中率；信息优势型结论（依赖非公开渠道的判断）不承诺复制，产出中显式标注"此结论类型存在信息层差距"。系统不假装复制 UP 的信息优势。

## 三、设计思想与总体架构

**设计思想**：方法论不变，执行者换人——且方法论本身也要先经市场验证才配被执行。v2.0 的"人 + 检查清单 → AI 引擎 + 同一张检查清单"不变；v2.1 增加一条：**检查清单里的每条推理模式必须带 validation 区块（历史命中率/适用环境/已知失效场景），未通过验证的模式不得进入 prompt**。

```
                        ┌─────────────────────────────────────┐
                        │  调度层：盘前 / 盘中7时点 / 收盘      │
                        └───────────────┬─────────────────────┘
                                        ▼
┌──────────┐   ┌──────────────────┐   ┌──────────────────┐   ┌──────────────────┐
│ 双路径引擎 │→ │ ① 产业逻辑引擎     │→ │ ② 信号与情绪引擎   │→ │ ③ 执行与复盘引擎   │
│ (路径分流) │   │ (路径A研究自动化)  │   │ (跟踪+验尸自动化)  │   │ (计划+纪律+校准)   │
└──────────┘   └──────────────────┘   └──────────────────┘   └────────┬─────────┘
       │ 每个标的/建议             │ 假设清单                │ 信号触发        │ 建议报告
       │ 先定路径                 │ (数值置信度)             │ +验尸结果       │ +交易日志
       ▼                        ▼                        ▼              ▼
┌──────────────────────────────────────────────────────────────────────────┐
│ 引擎⓪ 方法论蒸馏：推理模式提纯(来源中立) + 历史回测 + validation 区块        │
├──────────────────────────────────────────────────────────────────────────┤
│ 知识底座（三层）：                                                          │
│  · 产业链知识库（底座中的底座：环节/价值量/壁垒/格局/传导链/标的映射）        │
│  · 方法论库（蒸馏后、带验证记录的推理模式 + UP术语词典）                     │
│  · 观点对照库（claims 分桶：up教材/agent自产/research/announcement/data）   │
│ 数据底座：东财→新浪→缓存降级链 · K线SQLite缓存 · akshare财报 · 市场情绪      │
│           · 研报/公告/财报管线（扩容）                                      │
└──────────────────────────────────────────────────────────────────────────┘
```

关键事实（沿用 v2.0）：四大引擎所需的底层能力在本仓库均已存在并每日跑通——Qing-Agent 的 9 节点 LangGraph 工作流已有知识检索、市场分析、个股分析、魔鬼代言人、引用校验、复核节点；监控规则引擎已在做信号触发与去重。本项目是把它们按四大模块重组为统一系统，只新建编排层与各模块缺失的薄功能。

## 四、引擎⓪：方法论蒸馏引擎（新增，最高优先级）

**目标**：把 UP 的推理方法从"UP 个人表达"中分离出来，变成来源中立、数据驱动、可验证、可证伪的方法论资产。

### 4.1 推理模式提纯（改写 `framework/reasoning-patterns.yaml`）

现有 10 个通用框架（116 个单 raw 模式聚合而来）每条含 `UP_logic` 字段，逐条改写为来源中立结构：

```yaml
- pattern_id: bom-diffusion          # 例：BOM拆解扩散
  trigger:                           # 触发条件：客观数据特征，不再引用"UP说"
    - 主线核心资产进入高位轮动（核心标的涨幅/换手达阈值）
    - 下一代系统级产品 BOM 可拆解
  data_requirements:                 # 每步所需数据及获取通道
    - 环节价值量 ← 研报/产业链知识库
    - 供给壁垒与涨价能力 ← 公告/认证状态/涨价函
    - 位置（相对已涨核心） ← K线缓存
    - 订单/客户验证 ← 公告/财报/互动平台
  steps: [拆BOM → 比价值量增幅 → 筛供给壁垒 → 比位置 → 要订单验证 → 标的四层分层]
  falsification: [核心环节破位且资金离开主线, 订单验证连续落空]
  validation:                        # 市场验证记录，未填不得进 prompt
    historical_hit_rate: null        # 历史回测命中率
    applicable_regime: null          # 适用市场环境
    known_failures: []               # 已知失效场景
```

### 4.2 历史回测（补上缺失的 backtest 基建）

- 用 K 线 SQLite 缓存 + 550 篇 raw（作当时信息环境的语料）+ 451 张 claims 回放验证 10 个框架：在历史交易日只喂当天可得数据，跑推理模式，对照后续市场走势计命中率，填入 validation 区块；
- `docs/tasks/buy-signal-implementation.md` 引用的 `scripts/backtest_buy_signals.py` **并不存在**——回测基建是 M0 的新建项，不是复用项，如实估算工作量。

### 4.3 UP 概念体系解耦为术语词典

冰点、炸板率、缩容炒作、高低切等概念保留为**术语词典**（每个词给出客观数据定义，如"炸板率 = 当日炸板家数/触及涨停家数"），与方法论骨架解耦——概念是描述市场的语言，谁都能用；方法论是推理步骤，必须验证。

## 五、产业链知识库：底座中的底座（新增专节）

**定位**：UP 的涨价链推导本质是跑在产业链客观结构上的查询。把这个结构建成独立维护的知识库，AI 就能脱离 UP 自己做产业推导——这是本方案"脱离 UP"最核心的资产建设。

### 5.1 Schema（以仓库已有深度研究为模板）

`docs/标的深度研究/方向一：长鑫存储产业链全景梳理` 的结构已验证有效，固化为标准 schema。每条产业链一个条目，存放于 `knowledge/industry-chains/<chain-id>/`：

- **`chain.yaml`（机读）**：
  - `thesis`：一句话传导链（如"长鑫IPO融资扩产 → 资本开支扩大 → 设备/材料采购增加 → 封测配套需求提升"）；
  - `segments[]`：环节分层，每环节含 `name / value_share（价值量） / barrier（壁垒） / landscape（格局） / growth（增速） / status（当前状态：扩产中/涨价中/认证突破…）`；
  - `mappings[]`：标的映射，含 `code / name / segment / relation（与链主关系） / cert_status（认证/供货状态） / order_evidence（订单证据+来源+日期） / elasticity（核心受益/弹性/概念，及理由）`；
  - `last_verified`：各字段最近核实日期（产业链知识缓变但会过期，必须有保鲜期）。
- **`research.md`（人读）**：沿用现有深度报告格式（分赛道详表 + 投资要点 + 核心/弹性总结）。

### 5.2 数据源：客观信息供给，不由 UP 供给

| 数据 | 通道 |
|---|---|
| 公告/招标/扩产/参股关系 | 事件管线（`qing-event-pipeline`）、交易所公告采集 |
| 财报/营收/关联采购额 | akshare 财报 cron（`fetch_financial_reports_cron.py`） |
| 研报（环节价值量/壁垒/格局判断） | **研报管线扩容——当前全仓库仅 2 篇，是必须补的短板** |
| 价格/涨价函/现货价 | 监控规则引擎 + 事件管线 |
| 认证/供货状态 | 公告、互动平台问答、招股书 |

UP 对产业链的解读（如"存储是下一阶段弹性所在"）降级为**该库的一个推理模式的输出样例**，用来对照，不用来供给事实。

### 5.3 事件驱动维护（治"一次性快照"病）

当前 `docs/标的深度研究/` 的文档写完就不再更新。v2.1 改为事件管线驱动：

- 触发事件：涨价函、招标公告、扩产公告、业绩预告、认证突破、链主 IPO/再融资进展；
- 动作：更新 `chain.yaml` 对应环节 `status` 与标的 `order_evidence`/`cert_status`，刷新 `last_verified`；传导链状态变化（如"设备采购启动"）→ 自动触发对下游环节的重推导，产出进入引擎②的信号跟踪表；
- `last_verified` 超阈值的字段在报告中自动标注"待核实"，防止拿过期产业链知识当事实。

### 5.4 与推理引擎的关系

方法论库里所有产业链类模式（BOM 扩散、涨价传导五步、标的分层）的 `data_requirements` 直接指向本库——**模式提供推理步骤，知识库提供事实**，二者分离后各自可独立验证、独立更新。引擎① 的产业链拆解产出也回写本库，越用越厚。

### 5.5 数据获取与维护分工：AI 自动做什么，人做什么

| 字段类型 | 例子 | 获取方式 | 自动化程度 |
|---|---|---|---|
| 事实层 | 公告、招标、参股关系、财报、关联采购额、价格 | 事件管线 + 财报 cron + 行情降级链（仓库已有通道） | **AI 全自动** |
| 现货价/涨价函 | 存储现货价、封装涨价函 | 公开价格网站（生意社等）+ 公告/新闻采集 | AI 自动为主，部分源需人工确认 |
| 分析层 | 环节价值量、壁垒、格局、增速判断 | AI 从公开研报/券商摘要/招股书抽取，标注来源 | **AI 初稿 + 人工抽检**（研报管线扩容后自动比例提高） |
| 认证/订单核实 | "已供货"状态、订单证据 | AI 从公告/互动平台问答（互动易/e互动）抽取，附来源链接 | AI 抽取 + 关键字段人工确认 |
| 信息层 | 渠道调研、产业链人脉消息 | **AI 物理上没有此渠道** | 不参与；或用户手动填入，标注"人工信息源" |

- **冷启动**：存量 3 篇深度研究（已经人工验证）直接迁移为首批 `chain.yaml`；新增产业链走"AI 初稿 → 人工确认关键字段 → 入库"，跑顺后逐步提高自动比例；
- 用户手动填入的信息（如自有产业渠道）作为 `manual` 来源进 `mappings`，与 AI 抽取字段区分标注，同样受保鲜期管理。

## 六、双路径引擎：每笔建议先定路径

原框架铁律："同一标的必须事先选定路径 A 或路径 B，不能中途切换理由。"自动化设计：

- **路径判定**：每个进入观察池的标的带 `path` 字段（A/B），写入 `config/stock_monitor/watchlist.yaml`；AI 生成建议时必须声明路径，报告模板按路径分栏；
- **路径 A 建议**：只能引用基本面证据（订单、价格、产能、财报、产业链验证——v2.1 起优先取自产业链知识库）；失效条件必须对应假设证伪；
- **路径 B 建议**：只能引用情绪/趋势证据（成交额占比、换手率、舆论、估值分位）；失效条件必须是趋势破坏（止损价）；
- **混用检测（自动校验节点）**：复用 Qing-Agent 的 reviewer 节点加一条校验规则——若路径 A 建议里出现"情绪还在所以持有"类表述，或路径 B 建议里出现"基本面好所以长持"，判违规、退回重写。这对应原框架复盘表里的"路径纪律"一栏，从人工自查变成机器强制。

## 七、引擎①②③（骨架与纪律不变，接口按 v2.1 调整）

### 7.1 引擎①：产业逻辑引擎（模块一自动化）

**目标**：输入一个产业主题或标的，自动产出产业链拆解 + 结构化假设清单。

**产业链拆解**：
- 拆解脚手架从"UP 的推理链"换成**蒸馏后带 validation 的方法论模式**（第四节），事实取自产业链知识库（第五节）；`docs/标的深度研究/` 存量报告作为输出范例并迁移入库；
- 输出：上中下游分层、各环节价值分配/壁垒/格局、核心事件的"直接受益/间接受益/情绪映射/受损"四类标的映射，产出回写产业链知识库（5.4）。

**信息分级（自动标注）**：
- 按原框架三级标准给每条输入信息自动打级：一级（公告/财报/交易所）、二级（研报/协会）、三级（社媒/传闻）；
- 硬约束（原框架规则不变）：**任何假设至少 1 条一级 + 1 条二级信息支撑**，否则不进入假设清单。数据源降级链（东财→新浪→缓存→报错）保证一级信息的诚实性：没数据就报"数据缺失"，禁止 AI 编造；
- 现状短板：全仓库研报仅 2 篇，二级信息供给不足，该约束在知识层落空——研报管线扩容（M3）是前置任务。

**假设清单（自动生成，对接 claim 卡）**：
- 输出格式沿用原框架字段：**假设内容 / 判断依据 / 验证信号 / 信息等级 / 数值置信度(%) / 基准率 / 兑现时间窗 / 证伪后处理**；
- 置信度不是拍脑袋：AI 必须先在 `knowledge/cases/` 案例库检索历史类似案例（基准率），再给出数值；
- 直接写成 `knowledge/claims/` 的 claim 卡格式（statement/evidence/confidence/status/supersedes 天然同构），进入 Neo4j 观点演化图——**假设从此可检索、可追溯、可证伪**。

### 7.2 引擎②：信号与情绪引擎（模块二自动化）

**目标**：假设清单里的每条验证信号变成机器盯的指标；路径 B 的四指标自动计算；事前验尸自动执行。

**路径 A：基本面信号跟踪**：
- 把假设清单的验证信号（招标公告、价格库存、业绩预告、政策事件、产业链互证）映射到现有采集通道：监控规则引擎（`monitor/rules/`）盯价格/区间类信号，财报 cron（`fetch_financial_reports_cron.py`）盯业绩信号，事件管线（`qing-event-pipeline`）盯公告/政策类信号；v2.1 新增信号源：产业链知识库的事件驱动更新（5.3）；
- 信号触发 → 自动更新对应 claim 的状态与置信度（支持→上调、反对→下调）；**置信度跌破 40% 自动判假设失效**，按"证伪后处理"字段执行并通知——原框架"纠偏判断流程"的自动版。

**路径 B：情绪四指标（自动计算）**：
- 板块成交额占比、龙头股换手率、舆论密度、相对估值分位，全部由现有数据源计算（东财行情 + 板块数据 + 情绪采集 `market_sentiment`）；冰点、炸板率等术语按术语词典的客观定义（4.3）计算；
- 判定"升温/过热/退潮/冰点"时调用 `methodology/market-cycle.md` 与 `framework/market-cycle-framework.md` 的状态定义，避免 AI 自由发挥；量能真假用 `framework/volume-quality-assessment.md` 的三步法交叉验证。

**事前验尸（自动化 Devil's Advocate）**：
- Qing-Agent 已有 devils_advocate 节点，把原框架 pre-mortem 流程固化为输出契约：对每笔建议**必须输出"假设 6 个月后失败的 3 个具体死因"**，且每个死因回推一个"现在可观察的先行指标"并入路径 A 跟踪表；
- 原框架的 5 个反证问题（是否已 price-in、是否只听利好、空仓还会买吗等）作为该节点固定检查清单注入 prompt。

### 7.3 引擎③：执行与复盘引擎（模块三自动化）

**目标**：交易计划自动生成、仓位纪律机器强制、日志与复盘全自动、校准按季自动跑。

**交易前路径匹配检查（自动门禁）**：
- 原框架五问表变成建议报告强制字段：买入理由 / 持有周期 / 证伪信号 / 退出条件 / 最大亏损，缺一不出报告；
- "答案介于两条路径之间 → 不做"——reviewer 节点判不定路径时直接输出"回避"。

**仓位规则引擎（纪律硬编码）**：
- 原框架公式不变，机器强制换算：**仓位% = 单笔风险上限% ÷ 止损幅度%**，再套三层上限（个股 ≤10%、单主题 ≤20%、路径 B 合计 ≤30%）取最小值，实际风险重算确认 ≤2%；
- 每条建议在报告里必须同时出现**仓位/止损/单笔风险三个数值**，缺一被校验节点打回；
- 组合熔断：月回撤 ≥10% 自动触发"强制减半"建议——从人的自觉变成系统规则（持仓数据来自 `positions.yaml`，本地私有、不进 git）。

**交易日志与复盘（自动记录、过程/结果分离）**：
- 日志：每笔建议落盘即留档（标的/路径/三数值/对应假设/市场周期位置/数据时间戳），无需人记；
- 复盘：收盘后自动跑（现有 daily-review 通道），按原框架原则**过程评分与结果评分分离**——过程分评"当时信息下流程是否合格"，防止用结果反推决策质量（resulting 偏误）；
- 错误自动分类六型（逻辑/数据/执行/心理/路径/**运气**），"过程合格但亏损"自动归"运气"，防止 AI 因一次坏运气乱改方法论。

**季度校准（自动统计）**：
- 按季度统计所有已到时间窗的假设——"AI 说 70% 的事实际兑现几成"，按置信度分桶输出命中率偏差表；偏差超 10 个百分点 → 自动给下季度 AI 输出的置信度打 9 折（prompt 层注入校准系数）；
- v2.1 升级：从"校准 AI 置信度"扩展为"分桶校准各信息源"（up/agent/research/announcement/data 各桶分别统计，见第十二节）。

## 八、每日闭环（v2.0 不变 + 影子期增项）

| 时段 | 动作 | 涉及引擎 |
|---|---|---|
| 盘前 06:30 | 预拉 K 线/财报；生成《今日观察清单》（各标的的触发/失效条件，纯配置渲染不经 LLM） | 双路径 + 引擎② |
| 盘中 7 时点（9:26–15:05） | 信号引擎扫描 → 触发即走：路径判定 → 检索对应假设/claim → AI 分析（含事前验尸）→ 仓位换算 → 《盘中建议报告》 | 全部 |
| 收盘 15:30 | 复盘当日信号与建议 → 更新 claim 状态/置信度 → 《每日投资建议报告》→ 观点跨日连续 | 引擎②③ |
| **影子双轨期每日** | **AI 盲出结论（prompt 屏蔽 UP 最新观点）→ 收盘后与市场走势、UP 结论双向对照归因**（见第十节） | 引擎⓪①② |
| 每季度首日 | 分桶校准统计 → 命中率偏差表 → 调整置信度校准系数 | 引擎③ |

**降级原则**（贯穿所有引擎，不变）：AI 不可达 → 规则引擎照常出纯规则报告并标注"无 AI 推断"；数据源全挂 → 明确报错，禁止编造；幻觉校验不过 → 该段输出作废。**系统的底线行为与原框架红线一致：宁可没有建议，不给错的建议。**

## 九、输出契约（v2.0 不变，新增一条）

- 每条建议五要素：标的+方向 / 触发条件 / 失效条件 / 依据与证据分级（证据 vs 推断）/ 数据时间戳；
- 失效条件缺失 → 显式标注"未配置失效条件"，严禁编造；
- 报告头：数据时间、生成时间、分析引擎、路径分栏；报告尾：固定免责声明；
- 无无条件买卖指令（AGENTS.md 红线）；
- **新增**：依赖信息层判断的结论（见第二节）必须标注"此结论存在信息层差距，置信度上限受公开数据约束"；引用产业链知识库字段超过保鲜期的，标注"待核实"。

## 十、盲测对齐与毕业机制（新增，核心创新）

**目标**：用市场走势检验 AI 独立推理能力，达标后毕业脱离 UP，而不是 UP 一断更就被迫降级。

### 10.1 历史回放测试集

选 N 个历史交易日（覆盖上涨/震荡/下跌、主线清晰/混沌等不同环境），只喂当天可得的客观数据（K线、板块资金、公告、当时已有的产业链知识库快照），AI 独立输出市场阶段/方向/标的判断。**UP 当天说了什么不进 prompt。**

### 10.2 双对比与评分

- **主判据：vs 市场后续走势**——阶段判断对不对、方向涨没涨、标的相对收益；
- **参考：vs UP 当时结论**——只作诊断信息（AI 与 UP 分歧且 AI 对市场对的案例，是毕业信心的最强证据；AI 与 UP 一致但都错，说明方法论本身有坑，回炉引擎⓪）。

### 10.3 影子双轨期

每日流程：AI 盲出结论（prompt 屏蔽 UP 最新观点）→ 收盘后差异归因，归因分四型：

| 归因类型 | 含义 | 处置 |
|---|---|---|
| 数据缺 | 推理所需数据没有采集通道 | 补数据通道 |
| 步骤缺 | 方法论缺环节 | 回写引擎⓪ 补模式 |
| 概念误用 | 术语/框架用错场景 | 修订术语词典/prompt |
| 信息差 | 依赖 UP 非公开渠道信息 | 标注能力边界，不强求 |

### 10.4 毕业标准（量化）

连续 8 周（覆盖至少一次完整情绪周期）达标才执行第十一节的锚定拆除：

- 市场阶段判断与事后市场定性一致率 ≥ 70%；
- 方向判断（给出的 1-3 个方向）5 日内相对沪深300 超额为正的比率 ≥ 60%；
- 路径 A 建议的假设证伪率不高于历史基准 +10pct；
- 阈值未达标：留在影子期继续迭代，**不降级、不硬切**。

## 十一、prompt 层拆除 UP 实时锚定（修订 v2.0 红线）

毕业后执行，逐条改 `src/qing_investment/agent/prompts/`：

- `direction_signals`（UP 看好程度定方向优先级）、`up_sentiment`（UP 转谨慎则价格到位也等确认）：从"实时依据"降级为"事后对照标签"，仅用于复盘归因，不进决策链；
- "推理模式是你思考的方式，不是可选工具""你应该模仿这个逻辑框架" → "执行经过验证的方法论步骤，每步声明所用数据与来源"；
- 废除冲突裁决的"UP 优先"，改为**市场数据优先**（一级信息 > 二级 > 三级；价格/量能证据 > 任何观点）；
- reviewer 的"是否与 UP 立场矛盾"改为"是否与已验证证据矛盾"；
- **红线修订**：v2.0"只调用、不修改 `src/qing_investment/`"改为——**不动引擎逻辑代码，但 `prompts/` 是本次改造对象**；prompt 改动走与代码同等的评审与回归流程（evals 全绿才可合入）。

## 十二、知识底座重构：多源 + AI 自产 + 市场回写

- **claims 按 source 分桶**：`up`（教材，只用于训练与对照）/ `agent`（AI 自产）/ `research`（研报）/ `announcement`（公告）/ `data`（量价数据回写）；各桶权重**由该类历史命中率决定**，不再按"语言强度"设置信度；
- **AI 自产回路**：AI 每日结论按假设清单格式写 agent claims → 进引擎②验证回路 → 市场结果回写置信度（v2.0 引擎②的内部机制升级为底座级机制）；
- **评估脚本改造**：`evaluate_agent_vs_up.py` → `evaluate_vs_market.py`，面向市场的分桶命中率评估，**既评 AI 也评 UP**——副产品是"UP 各类型观点准确率画像"（哪类判断 UP 历来准、哪类历来不准），教材按类型加权使用：UP 准的类型多学，不准的类型即使语言再自信也降权；
- **信息源扩容**：研报/公告/财报采集管线充实（当前研报仅 2 篇），让信息分级硬约束在知识层真正可执行。

## 十三、与仓库现有资产的映射（更新）

| 模块 | 复用 | 新建 |
|---|---|---|
| 引擎⓪ 蒸馏 | `reasoning-patterns.yaml`（含 v1 备份）、550 篇 raw、K线缓存、451 claims | **回测基建**（`backtest_buy_signals.py` 不存在，需新建）、validation 区块结构、术语词典 |
| 产业链知识库 | `docs/标的深度研究/`（schema 模板与存量内容）、事件管线、财报 cron、`sector-diffusion-framework.md` | `knowledge/industry-chains/` 目录与 chain.yaml schema、事件驱动更新钩子、**研报采集管线扩容** |
| 双路径 | `methodology/technical-analysis.md`、reviewer 节点 | watchlist `path` 字段、混用校验规则 |
| 引擎① | claims 管线（`qing-learning-claim`） | 假设清单→claim 卡自动转写 |
| 引擎② | 监控规则引擎、财报 cron、事件管线、`market_sentiment`、devils_advocate、market-cycle/volume-quality 框架 | 信号→claim 置信度自动更新；pre-mortem 输出契约 |
| 引擎③ | daily-review、daily_state、positions.yaml、output-contracts | 仓位三数值强制校验、错误六型自动分类、分桶校准统计 |
| 盲测毕业 | K线缓存（历史回放数据源）、evals 框架 | 历史回放测试集、影子双轨流程、差异归因与毕业判分 |
| 数据/推送 | 东财→新浪降级链、K线缓存、微信推送 | 统一编排入口与调度样例 |

## 十四、实施路线（重排：验证先行）

| 里程碑 | 内容 | 验收标准 |
|---|---|---|
| **M0 蒸馏+回测基建** | 推理模式来源中立改写；术语词典；回测脚本新建；产业链知识库 schema 落地并迁移存量 3 篇深度研究 | 10 个框架全部带 validation 区块；回放能跑出命中率 |
| **M1 盲测 eval** | 历史回放测试集；双对比评分 | 给出 AI 独立推理的基线命中率报告 |
| **M2 影子双轨**（并行原 M1-M3：双路径/执行复盘/信号引擎） | 每日盲出结论 + 收盘差异归因；归因四型回写 | 连续 4 周归因记录完整，数据缺/步骤缺类问题有闭环 |
| **M3 信号回写底座化** | claims 分桶；市场结果回写置信度；`evaluate_vs_market.py`；研报管线扩容 | 分桶命中率表产出（含 UP 画像）；一级+二级信息约束可执行 |
| **M4 毕业切换（拆锚定）** | 达毕业标准后执行第十一节 prompt 改造；evals 回归 | 连续 8 周达标；prompt 改造后 evals 全绿 |
| **M5 校准常态化** | 季度分桶校准；产业链知识库保鲜巡检 | 首期命中率偏差表；无超保鲜期未标注字段 |

对 v2.0"校准最后因为需要数据积累"的修正：**451 claims + 550 raw + K线缓存已是现成积累，缺的不是数据是回写机制**，故校准相关基建前移到 M3。

> **v2.2 注**：上表为 v2.1 原始路线；影子首周实证后，遗留事项已按"立即可做 / 等数据 / 等毕业"重排，执行以**第十六节 16.6** 为准。

## 十五、边界与红线（修订）

- 不接交易接口、不自动下单；`positions.yaml` 等私有数据不进 git、不进 prompt 外发；
- 不承诺收益；所有产出保留"非投资建议"声明；
- 原框架四条铁律（路径不混用、止损无条件、仓位有上限、数据要诚实）是硬编码约束，不是建议；
- **诚实边界**：UP 的信息层优势（渠道调研等非公开信息）AI 物理上没有，依赖此类信息的结论类型只承诺公开数据逼近，产出中显式标注，不假装复制；
- **修订**：v2.0"只调用、不修改 `src/qing_investment/`、`scripts/`、`config/stock_monitor/`"——引擎逻辑代码与配置仍只调用不修改；**例外**：`src/qing_investment/agent/prompts/` 属 M4 改造对象（走评审+evals 回归），回测/评估类脚本（`scripts/backtest_*.py`、`scripts/evaluate_*.py`）属新建与改造范围。

---

## 十六、v2.2 修订：影子首周实证后的六处调整（2026-08-15）

> 实证输入：影子双轨 6 个交易日（08-07~08-14，含 2 个早盘 -pre 记录）。机制按设计兑现：契约一周内 v2→v5 迭代（补 scenarios/watch_next/invalidation/operation/cycle_state/盘中形态/单位纪律），全部由具体错误驱动；归因产出数据缺/步骤缺提案；factcheck 抓到 watch_next 编造（08-14 提及不在涨停池的景旺电子/生益科技）。同时暴露以下需修订项。

### 16.1 修订一：毕业判据修正——阶段一致率降权，超额收益升主判据

**问题（实证）**：首周阶段判定 6/6 全对，但证据效力弱——① 本周真值全是"震荡"，单类别周，永远猜震荡也是满分；② 阶段真值是机械规则（`blindtest/truth.py`：r20/pos20/vol_trend），模型数据包里的指数 K 线与真值同源，`stage_hit` 本质是"模型与规则读同一份数的一致性"，有循环论证成分，不能当预测力证据。原第十节毕业标准把"阶段一致率 ≥70%"列为首条，会被震荡市刷高。

**方案**：
- 毕业判决主判据改为**无循环的两条**：方向 5 日超额命中率 ≥60%（维持原标准）、标的 5 日超额命中率 ≥55%（对齐 M1 基线 56.6% 设门槛）；
- 阶段一致率降级为**参考项**，报告中必须标注"与机械真值的一致性检查，非预测力证据"；
- 主判据加**分环境段约束**：按真值标签分段统计，每段（主升/震荡/调整/恐慌）样本 <10 该段不计入；窗口内环境单一（如纯震荡）→ 维持 `insufficient_data`，不毕业。此约束写进 `shadow/graduation.py` 聚合逻辑；
- 原判据"路径 A 假设证伪率"维持 pending-definition（待 M3 分桶，见 16.6）。

**验收**：`graduation_check.py` 输出含分环境段明细与单环境警示；毕业/未毕业判决只由超额两条决定。

### 16.2 修订二：重跑-归因联动闭环（修 bug）

**问题（实证）**：08-13 预测因数据缺陷误判"调整"触发归因，产出 2 份 open 提案；当日数据修复后重跑（契约 v3）判"震荡"正确，但**旧归因与提案未联动失效**——`evals/shadow/attributions/2026-08-13.json` 与两份 open 提案的证据基础已不存在，提案制被脏数据污染。

**方案**（本次落地实现）：
- `shadow/predict.py` 增加 `force` 重跑通道：覆盖已有预测前，若该日存在归因记录，标记 `superseded: true`（保留原文留痕），并将其 `proposal_refs` 中仍为 open 的提案改 `status: retracted` 并注明原因；重跑记录重置为 `pending_maturity`（stage_hit/due_scores 清空重评）；
- `shadow_daily.py` 透传 `--force`；`shadow/status.py` 统计排除 retracted 提案、归因合并时忽略 superseded 旧记录；
- 存量处置：08-13 归因标记 superseded，两份 open 提案（`2026-08-13-data-channel-note.md`、`2026-08-13-pattern-patch-note.md`）改 retracted（板块资金流/分时通道缺口已由 `KNOWN_DATA_GAPS` 登记，转入 16.4 数据通道议题，不丢）。

**验收**：force 重跑场景有单测覆盖；08-13 存量处置落盘后 status 报告 open 提案数正确；重跑后若仍判错，归因按新记录重新触发。

### 16.3 修订三：qing-agent 对比臂——直接复用现有复盘 JSON，不做 qing 盲跑

**问题**：原设想（M2.5 个股 A/B）需要给 qing-agent 也做盲跑对比，成本高。**调查结论：不需要**——qing-agent 每日 17 点复盘任务已产出结构化 JSON：`config/stock_monitor/daily_review_summary.json`，按日期键累积 46 个交易日（2026-06-11~08-14），每天含 `market.stage`（阶段）、`direction_priority`（方向列表）、持仓标的，机械可解析。

**方案**：
- 新建评分脚本（`investment_engine` 模块）：读 `daily_review_summary.json`，将 `stage` 自由文本（46 天约 26 种写法，7 天"未判断"剔除）归一化到四枚举，用**同一份** `truth.py` 机械真值评分；产出 qing 臂 46 天阶段一致率，与 shadow 同窗对比；
- 方向臂受限如实标注：`direction_priority` 为自由文本方向名、无次日推荐标的的结构化输出（`tomorrow_scenarios` 几乎全空），标的维度不评分，方向维度 V1 只做人工抽检对照；
- **对比口径声明写进报告头**：qing 臂 = UP 锚定 + 收盘后信息（非盲）的重管线；shadow 臂 = 盲判轻管线。两者比较服务 M4 拆锚定决策——若 shadow 臂不劣于 qing 臂，拆锚定与 qing 分析层退役即获证据支持；
- 个股分析 A/B（原 M2.5）缓办：等 shadow 标的超额样本 n≥20 再启动。

**验收**：产出 46 天 qing 臂 vs shadow 同窗的阶段一致率对比表（含口径声明）；该表纳入每周五 `graduation_check.py` 例行查看。

### 16.4 修订四：研报管线定案——akshare 公开源为主，KPL 本地初调，人工投递兜底

**调查结论（实测）**：KPL **已封装端点中没有**服务端关键词搜索接口，资讯只有"按日时间流列表→逐篇全文"，且最像研报的付费专栏（券商研报转载）全文 1130 无权限、只能拿标题/摘要；TDX（pytdx 直连）是纯行情协议，**无资讯/研报能力**，排除。**App 内搜索接口已于 2026-08-15 晚抓包实测为不可得**（证书固定原生通道/直连 socket，见 `docs/design/kpl-api-inventory.md`「搜索/文章接口抓包」节），KPL 搜索接入放弃；但同次抓包发现 **H5 `PContent2.html` 链路可读公众号转载长文全文**（绕开 1130），产业链深度文主要载体可落盘。**akshare spike（2026-08-15 实测通过）**：`stock_research_report_em` 个股研报列表可用（机构/评级/日期/盈利预测，**PDF 直链可下载**，002916/600183 等最新报告覆盖到 2026 年）；`stock_notice_report` 日更公告可用（顺带补上 KNOWN_DATA_GAPS 的"公告流"）。**底层东财 API 检索能力实测（akshare 未封装层）**：`reportapi.eastmoney.com/report/list` 支持 `code=*` 全量列表 + qType（个股/行业/策略）+ **industryCode 行业过滤（实测 459=元件精确命中）** + 日期/机构/评级过滤；自由关键词参数无效（keyWord/keyword/searchKey 均忽略）。**东财站搜索 API**（`search-api-web.eastmoney.com`，需 Referer 头）支持自由关键词搜索新闻/文章（实测"PCB产业链"命中上证报/每经/证券日报当日文章）——资讯级搜索通道成立。当前可行方案三层：

**方案**：
- **主力源：akshare 东财研报/公告接口**（`stock_research_report_em` 等）——akshare 已是项目依赖、接口从未启用、公开源无封号风险。~~先 spike 验证当前版本可用性~~（spike 已通过）。**管线 v1 已落地（2026-08-15）**：`src/investment_engine/research_feed.py` + `scripts/fetch_research_reports.py`，研报走 report/list 原始 API（qType 个股/行业/策略全量，元数据含 PDF 直链），公告走 akshare 公告大全；落盘 `infra/data/research/{reports,notices}/<日>.json`（gitignored），幂等日更，PDF 按需下载（`download_pdf`）；
- **KPL 本地初调**：资讯已逐日全量落盘（`infra/data/kpl/news/<day>/`），在其上加本地关键词过滤层（"产业链"等 + 方向池关键词），命中条目全文已在本地（含公众号转载长文，H5 链路），产出每日初调摘要；付费专栏条目留标题/摘要/关联股票作线索。**初调层 v1 已落地（2026-08-15）**：`kpl/digest.py` + `scripts/kpl_news_digest.py`（产业词基表 + 股票池个股名过滤标题，分组"全文可读/仅标题线索"，落盘 `infra/data/kpl/digest/<day>.md`，cron 17:55）；
- **人工投递通道**：用户自做调研报告放 `sources/research/incoming/` 同构目录，与采集层走同一解析（`industry_chain/migrate.py` 的 `parse_research_md`），回写产业链库并刷新 `last_verified`；
- 由此引擎①"至少 1 条一级 + 1 条二级信息"硬约束在知识层真正可执行。

**能力边界与存量登记（2026-08-15 更新）**：已验证通道供给三类内容——券商研报列表+PDF（东财 report/list，行业代码过滤）、媒体新闻/资讯（东财站搜索关键词检索、KPL 日更）、公告（stock_notice_report）。**产业链深度分析类内容（BOM 拆解、链条梳理、供需格局长文）正式定性为人工来源**：已验证自动通道无直接供给（东财行业研报只到"行业"粒度，站搜索命中媒体资讯，KPL 搜索实锤不可得），不作为系统缺口追踪。**存量资产已登记**：
  - `docs/标的深度研究/`：3 方向深度梳理（长鑫存储/国产算力/AI 基础设施与能源转型，md+html）+ 长鑫扩产节点分析，已迁移入 `knowledge/industry-chains/`（changxin-dram / domestic-compute / ai-infra-energy 3 条 chain.yaml）；
  - `docs/` 根目录 4 份深度报告（AI PCB Rubin 产业链 PDF、AI 产业链全景-物理 AI 与具身智能、AI 基础设施与能源转型、国产算力与 Token 经济学）；
  - `sources/raw/财经/研报：` 系列约 70 篇（2026-01~05），其中 6 篇已提取 17 张 `source_type=研报` claims（AI PCB Rubin 7 张、半导体设备零部件 3 张等）；
  - `sources/research/`：高盛 2 篇（md 摘要 + PDF 原文，index.md 登记）。
  新增深度分析一律走人工投递（`sources/research/` 登记 index.md，或 `sources/incoming/` 待整理），经 `parse_research_md` 回写产业链库、按 claim 管线提取；新自动内容源由用户探索中，到手再验。

**验收**：研报/公告每日落盘 + KPL 初调摘要产出；新增产业链条目能引用到非 UP 的二级信息源。

### 16.5 修订五：分析层 skill 化——skill = 方法论 + 查询脚本 + 输出契约

**定位**：影子首周证明"智能"在三件套里——system prompt（契约+纪律）、数据包、结算回路；LangGraph 9 节点编排对判断任务是过度工程。qing-agent 分析层（人格化/reviewer 循环/三库检索）在拆锚定方向下既重且过时，其演进方向是 skill 化；监控层（采集/降级链/规则引擎/门控）是数据工程，**保留不动**。

**方案**：
- 新建 `skills/qing-market-analysis/`：`SKILL.md` 承载方法论（引用蒸馏模式库 + 输出契约 + 四条纪律），`scripts/` 为薄封装查询脚本（K 线/情绪/涨停池/产业链库/资讯，复用 `investment_engine` 与 `qing_investment.kline_cache` 现有数据访问层），LLM 在 skill 指引下按需调用查询脚本；
- **双形态同一事实源**：盲测/影子继续用**确定性整包**（可复现、有防泄漏断言、单次调用可结算）不动；交互式分析（"分析一下 XX"）走 skill 按需查询——两者共享同一数据访问层，避免双写双份事实；
- skill 产出遵守同一输出契约（阶段/方向/失效条件/数据时间戳），可打 `trigger=interactive` 标签落 `evals/shadow/predictions/` 进同一结算回路；
- devils_advocate 事前验尸降级为 skill 内的一段固定检查清单（不独立节点、不独立模型）；qdrant/neo4j/mem0 三库检索不进 skill（边际收益无证据），若后续需要知识检索再单独立项。

**验收**：skill 可查 K 线/情绪/涨停池/产业链四类数据；一次交互式个股分析产出符合输出契约并可被 maturity 结算。

### 16.6 修订六：M1/M2/M3 遗留事项重排（按依赖分组）

**立即可启动（无依赖，影子首周已具备条件）**：
| 事项 | 来源 | 动作 |
|---|---|---|
| 涨停池/情绪历史回填 | M1 spec 数据缺口 | **已实测：东财涨停池历史仅保留约 1 个月（边界 2026-07-27），无法回填至 04-27**；已回填 07-27→08-12 共 13 天，改为子窗口 A/B 对照（`scripts/blindtest_lp_ab.py`，同窗口同 prompt 仅 limit_pool 有无两臂）看命中率变化 |
| 东财 LHB 历史回填 | eastmoney-lhb spec 范围外项 | `--date` 逐日补拉 |
| 研报管线 | 主计划 M3（实际不依赖影子数据） | **v1 已落地（2026-08-15）**：`research_feed.py` + `fetch_research_reports.py`，回填 04-27 起；日更 cron 待挂（wrapper 见 m-series 文档） |
| `knowledge/cases/` 案例库扩容（仅 2 篇） | M0 验收遗留 | 从 550 篇 raw + 影子归因提炼，基准率检索可用 |
| 中证2000/微盘股指数入包 | contract-v2 spec D7 可选项 | 指数自补机制已通，直接加 |
| qing 对比臂评分 | 本稿 16.3 | 新建评分脚本 |
| 产业链保鲜巡检器 | M5 提前（status 文档原话"可随时提前补"） | 写巡检器：`last_verified` 超阈值字段自动标"待核实" |
| KPL 资讯分页、LHB 披露边界补记 | kpl plan 注、contract-v2 风险节 | 小项，顺手做 |
| KPL 搜索/文章接口抓包 | v2.2 §16.4 | **已完成（2026-08-15）**：搜索不可得（证书固定），H5 链路全文可读绕开 1130；记录见 kpl-api-inventory.md |

**等数据（影子满 ~4 周，约 09 月初）**：claims 分桶、市场结果回写置信度、`evaluate_vs_market.py`（代码基建可先行，画像等数据）、UP 命中率画像、3 个 pending-m1 未使用模式样本（n≥20 出桶）、9 份模式提名提案窗口验证（最早批次 09-08 后）、连续 4 周归因记录（M2 验收口径，进行中 6/20）。

**等毕业**：UP 每日对照扩展（M4）、毕业判据 3"路径 A 假设证伪率"定义（依赖 M3 分桶落地）。

**人工来源（已定性，非缺口）**：产业链深度分析内容（16.4，2026-08-15 登记存量）——投递走 `sources/research/`（登记 index.md）或 `sources/incoming/`；新自动源用户探索中。

**到期即评**：market_summary prompt 瘦身复盘（morning-pipeline-fix spec 一周观察期，2026-08-18 期满）。

### 16.7 首周实证的其他如实记录

- 到期结算仅 08-07 一批（方向 2/3、标的 3/4 跑赢基准），样本过小不作结论；下周批次到期后才有讨论价值；
- 08-10 人工三方对照已暴露"结果对但结构错"类问题（选电力防御 vs 当日情绪抱团+医药进攻）——此类问题只有对照才抓得到，方向臂的人工抽检（16.3）即为此设；
- M1 caveat 中 DeepSeek 知识截止未经官方确证一事维持开放，不阻塞；
- 云部署遗留：KPL 拉取云端 cron 未挂（缺凭据），本机三条测试 cron 注销义务不变（见 ops 文档）。

# INCLUDE-002 向下包含合并（课62）
# claim-20070630-001-b：向下时取两 K 线最低点当低点、两高点中较低者当高点合并。
# 构造：bar1/bar2 有包含（bar2 全在 bar1 内），方向向下（d1=9.2 <= d0=10.0），
#   向下合并得 M=[h 10.2, l 9.2]（低 9.2 来自原始 bar1）。
#   合并后序列 [11.5,10.0] / [10.2,9.2] / [10.8,9.6] 恰成底分型；
#   不合并则无分型（bar2 高点 10.2 < bar1 高点 10.4，双最低条件不成立）。
# idx 约定同 INCLUDE-001（极值所在原始 bar=1）。末尾未确认 → sure: false。
case_id: INCLUDE-002
claim_refs:
  - claim-20070630-001-b
bars:
  - [11.0, 11.5, 10.0, 10.2]
  - [10.2, 10.4, 9.2, 9.4]
  - [9.6, 10.2, 9.5, 9.8]
  - [9.8, 10.8, 9.6, 10.5]
expect:
  fx:
    - {idx: 1, type: down, sure: false}
